rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /benchmark-audio/{fileName} {
      allow read: if true;              // candidates play audio unauthenticated
      allow write: if request.auth != null;   // admin only, via mintBenchmarkAdminToken
    }
  }
}
